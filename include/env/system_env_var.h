#ifndef _ENV_SYSTEM_ENV_VAR_H_
#define _ENV_SYSTEM_ENV_VAR_H_

#include <iostream>
#include <string>
#include <cstdlib>
#include <algorithm>

#include "env/system_var.h"

int read_env_var() {
    // const char *ENV_VAR = "HOME";
    // const char *tmp = _dupenv_s("HOME");
    // std::string env_var(tmp ? tmp : "");
    // if (env_var.empty()) {
    //     std::cerr << "[ERROR] No such variable found!" << std::endl;
    //     exit(EXIT_FAILURE);
    // }

    // std::cout << "HOME : " << env_var << std::endl;

    // exit(EXIT_SUCCESS);
    return 0;
}

#endif