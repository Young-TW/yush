#!/usr/bin/yushs
# yush install script

# build

echo "start Build"

cmake -B build

cmake --build build --config RELEASE

# install

cmake --install build --config RELEASE

echo "yush installed successfully"